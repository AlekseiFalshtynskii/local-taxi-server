server:
  port: 5010

spring:
  profiles:
    active: local
  application:
    name: auth-service
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
  liquibase:
    change-log: classpath:db/database_changelog.xml
